# Dependencies
node_modules/

# Build output
dist/
out/
release/
*.tsbuildinfo
tmp/

# Local Cursor / editor instructions at repo root (repo에 올리지 않음)
/AGENTS.md
.cursor/
CONTEXT.md
.impeccable/
.codex/
test/
# Local secrets and machine-specific config — never commit
.env
.env.*
!.env.example
ai.toml
!.ai.toml.example
*.pem
*.key
*.p12
*.pfx

# Google Cloud OAuth client JSON (Console에서 다운로드한 파일)
client_secret*.json
*-credentials.json

# OS / Electron credential files (userData 또는 실수로 복사된 파일)
*.cred

# Local SQLite (runtime userData / 실수로 루트에 생긴 경우)
*.db
*.db-wal
*.db-shm

# Logs & caches
*.log
npm-debug.log*
.vite/
.cache/
coverage/
/test-results/
/test/test-results/
/test/product-qa/runs/

# OS
.DS_Store
Thumbs.db
Desktop.ini

# IDE
.idea/
.vscode/
*.swp
.kiro/

# Local document artifacts (runtime)
.ax-studio/

# Python document-engine
packages/document-engine/.venv/
**/__pycache__/
*.py[cod]
