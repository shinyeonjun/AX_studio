/**
 * Data nodes — filter, field pick/rename, value lookup.
 *
 * Planned capabilities:
 *   - data.filter
 *   - data.field.map
 *   - data.pick
 *
 * Agent inserts these from natural language; user does not pick "Formatter" manually.
 *
 * @see ../README.md
 */
export {};
