/**
 * HTTP escape hatch — generic API without dedicated connector.
 *
 * v1:
 *   - http.get   — sideEffect NONE, read-only
 *
 * Later:
 *   - http.post  — allowed hosts + credential ref + EXTERNAL/EXTERNAL_HIGH
 *
 * @see ../../nodes/README.md
 */
export {};
