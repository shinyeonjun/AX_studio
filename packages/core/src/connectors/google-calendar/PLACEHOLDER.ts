/**
 * Google Calendar connector (v1.5 SaaS pack).
 *
 * Planned capabilities:
 *   - calendar.events.list
 *   - calendar.events.create
 *   - calendar.events.update (later)
 *
 * Demos: today's schedule summary, meeting create, conflict check.
 * OAuth: reuse Gmail Google OAuth client where possible.
 *
 * @see ../../nodes/README.md
 */
export {};
