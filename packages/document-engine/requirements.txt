# Minimal runtime for document-engine sidecar (dev + fallback parsing).
# 5.x contains known malformed-PDF resource-exhaustion vulnerabilities.
pypdf==6.16.1
pypdfium2>=4.30,<5
PyMuPDF>=1.24,<1.29
reportlab>=4.0,<5
Pillow>=10,<13
opencv-python-headless>=4.9,<5

# Full layout/OCR pipeline - install separately:
#   pip install -r requirements-docling.txt
